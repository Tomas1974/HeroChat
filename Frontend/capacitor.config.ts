import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Super_hero_chat',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
